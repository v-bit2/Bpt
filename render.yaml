services:
  - type: web
    name: viralbot-mini
    env: node
    plan: free
    buildCommand: npm install --omit=dev
    startCommand: node index.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
